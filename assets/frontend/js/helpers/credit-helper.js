const CreditHelper = {
  // getBookingStatusFromClass: (statusClass) => {
  //   const statusOption = statusOptions().find(
  //     (option) => option.value === statusClass,
  //   );
  //   return statusOption ? statusOption.label : "Unknown Status";
  // },
};

export default CreditHelper;
